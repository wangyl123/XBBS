<div class='box'>
    <div class='box-header'>
        分类节点
    </div>
    <div class='inner'>
        @{
            if (ViewBag.Categry != null && ViewBag.Categry is List<XBBS.Models.Category>)
            {
                foreach (var item in (List<XBBS.Models.Category>)ViewBag.Category)
                {
                    <p><span class="text-muted">@item.CName </span></p>
                    <p>
                        <?php if(isset($catelist[$v['cid']])) foreach($catelist[$v['cid']] as $c){?>
                        <a href="<?php echo site_url($c['flist_url']);?>"><?php echo $c['cname']?></a>&nbsp;
                        <?php }?>
                    </p>
                }

            }
        }

    </div>
</div>